apiVersion: v1
kind: Service
metadata:
  name: fds-api
  namespace: fds
  labels:
    app.kubernetes.io/name: fds-api
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: fds-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
      nodePort: 30080
