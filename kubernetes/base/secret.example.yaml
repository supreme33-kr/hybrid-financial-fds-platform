apiVersion: v1
kind: Secret
metadata:
  name: fds-api-secret
  namespace: fds
type: Opaque
stringData:
  # Do not commit a real password. Apply a locally created Secret in the actual environment.
  DATABASE_URL: "postgresql://fds_app:CHANGE_ME@10.1.93.53:5432/fdsdb"
