apiVersion: v1
kind: Secret
metadata:
  name: fds-api-secret
  namespace: fds
type: Opaque
stringData:
  # Create the real Secret from GitHub Actions or oc CLI. Do not commit production values.
  DATABASE_URL: "postgresql://fds_app:CHANGE_ME@REPLACE_ME:5432/fdsdb"
